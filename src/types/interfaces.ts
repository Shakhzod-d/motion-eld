export interface LoginData {
  login: string;
  password: string;
}

export interface IftaReportColumns {
  key: string;
  vehicle: string;
  state: string;
  melis: string;
}
export interface ArticleProps {
  id: number;
  title: string;
  text: string;
  status: string;
  desc: string;
  time?: string;
}

